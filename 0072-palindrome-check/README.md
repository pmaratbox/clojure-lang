# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. `clojure.string/reverse` gives the reversed string; `=` compares it to the original by value.

## Run

    clojure -M main.clj
