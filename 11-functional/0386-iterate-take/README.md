# 0386 — Iterate and Take

Iterate f(x)=x*3 from 1 and take the first four values, printing `1 3 9 27`. Clojure's `iterate` builds a lazy infinite seq that `take` truncates.

## Run

    clojure -M main.clj
