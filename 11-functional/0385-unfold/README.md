# 0385 — Unfold

Unfold a sequence by doubling from 1, taking five terms, printing `1 2 4 8 16`. Clojure's lazy `iterate` unfolds a seed by repeatedly applying a step function.

## Run

    clojure -M main.clj
