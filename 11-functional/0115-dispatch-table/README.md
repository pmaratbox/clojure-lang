# 0115 — Dispatch Table

Store functions in a map keyed by name, then apply "add" and "mul" to (3,4), printing `7 12`. A Clojure map holds the core functions `+` and `*` directly as values, looked up by string key.

## Run

    clojure -M main.clj
