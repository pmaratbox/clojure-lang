# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. Clojure's `map` is variadic, so `(map + xs ys)` zips and adds in one step.

## Run

    clojure -M main.clj
