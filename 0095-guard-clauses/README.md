# 0095 — Guard Clauses

Classify each of `-1`, `0`, and `5` using guard clauses (an early return for each special case) and print `negative`, `zero`, and `positive`. Clojure functions return their last expression and have no early `return`; `cond` expresses the same guard structure clause by clause.

## Run

    clojure -M main.clj
