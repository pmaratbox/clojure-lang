# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. The `->` threading macro feeds each immutable map step into the next, giving a fluent builder without mutation.

## Run

    clojure -M main.clj
