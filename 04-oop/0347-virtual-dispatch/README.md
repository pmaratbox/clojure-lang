# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. A defprotocol with per-record method bodies gives idiomatic dynamic dispatch.

## Run

    clojure -M main.clj
