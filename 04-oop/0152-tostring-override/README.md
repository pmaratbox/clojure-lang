# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. A `defrecord` can implement `Object.toString`, the Clojure equivalent of overriding a type's string form.

## Run

    clojure -M main.clj
