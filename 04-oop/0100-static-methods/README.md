# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. Clojure has no classes; a namespaced top-level function is the equivalent of a static method.

## Run

    clojure -M main.clj
