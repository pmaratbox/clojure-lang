# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. A shared atom incremented in the construction function serves as the class-level counter.

## Run

    clojure -M main.clj
