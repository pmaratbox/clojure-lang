# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. A plain `from-hex` function acts as the factory, parsing hex pairs into a Color record.

## Run

    clojure -M main.clj
