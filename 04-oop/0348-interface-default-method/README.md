# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. A protocol with a reify default and a record override models the default-vs-override split.

## Run

    clojure -M main.clj
