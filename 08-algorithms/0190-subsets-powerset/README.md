# 0190 — Subsets (Power Set)

Generate the power set of [1,2,3] in bitmask order 0..7 (empty printed as `{}`), one subset per line space-separated. `bit-test` checks each bit of the mask to decide membership.

## Run

    clojure -M main.clj
