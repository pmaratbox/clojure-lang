# 0191 — Pascal Triangle

Print the first 4 rows of Pascal’s triangle, each row space-separated. `iterate` builds each row by summing shifted copies of the previous one.

## Run

    clojure -M main.clj
