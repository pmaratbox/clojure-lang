# 0371 — Counting Sort

Counting-sort [3,1,2,3,1] ascending, printing `1 1 2 3 3`. Idiomatic Clojure tallies with `frequencies` then emits each value `repeat`-ed in value order.

## Run

    clojure -M main.clj
