# 0067 — Prefix Sums

Compute the running totals (prefix sums) of `1, 2, 3, 4` — each element added to the sum of all the previous ones — and print them: `1 3 6 10`. `reductions` returns the lazy sequence of intermediate accumulations of `+` over the input.

## Run

    clojure -M main.clj
