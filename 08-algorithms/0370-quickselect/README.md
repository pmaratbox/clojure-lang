# 0370 — Quickselect

Find the 3rd smallest element of [7,10,4,3,20,15] with quickselect, printing `7`. Idiomatic Clojure partitions with `filter` and tail-recurses into one side via `recur`.

## Run

    clojure -M main.clj
