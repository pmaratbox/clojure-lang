# 0193 — Happy Number

Determine whether 19 is a happy number and print `yes`. A `loop` sums the squares of the digits, using a `set` of seen values to detect cycles.

## Run

    clojure -M main.clj
