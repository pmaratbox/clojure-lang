# 0181 — Insertion Sort

Insertion-sort [5,1,4,2,8] ascending, printing `1 2 4 5 8`. Each `reduce` step splits the sorted prefix and inserts the next element in place.

## Run

    clojure -M main.clj
