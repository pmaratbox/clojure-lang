# 0367 — Selection Sort

Selection-sort [5,1,4,2] ascending, printing `1 2 4 5`. Idiomatic Clojure threads an immutable vector through `loop`/`recur`, using `reduce` to find the minimum index.

## Run

    clojure -M main.clj
