# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. A `for` comprehension picks each element in sorted order and recurses on the remaining ones.

## Run

    clojure -M main.clj
