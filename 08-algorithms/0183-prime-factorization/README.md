# 0183 — Prime Factorization

Factor 60 into primes, printing `2 2 3 5`. A `loop` divides out each factor with multiplicity, advancing the divisor when it no longer divides.

## Run

    clojure -M main.clj
