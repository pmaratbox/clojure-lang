# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. Clojure models an enum as an ordered vector of keywords, and `name` recovers each keyword's text.

## Run

    clojure -M main.clj
