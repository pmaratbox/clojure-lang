# 0375 — Retry With Attempts

Retry an operation that fails on attempts 1 and 2 and succeeds on attempt 3, printing `ok after 3`. A `loop`/`recur` drives the retries and reports the successful attempt count.

## Run

    clojure -M main.clj
