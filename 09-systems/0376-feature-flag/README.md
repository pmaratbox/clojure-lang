# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. A single `if`-based function is evaluated under `true` then `false`.

## Run

    clojure -M main.clj
