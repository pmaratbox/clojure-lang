# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. Clojure's `try`/`catch`/`finally` runs the `finally` form whether or not an exception was thrown.

## Run

    clojure -M main.clj
