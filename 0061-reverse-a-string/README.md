# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. `clojure.string/reverse` returns the string reversed (it delegates to `StringBuilder.reverse`, so it is surrogate-aware).

## Run

    clojure -M main.clj
