# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. Recursing on `rest` and appending `first` to the tail's reversal builds the result without `reverse`.

## Run

    clojure -M main.clj
