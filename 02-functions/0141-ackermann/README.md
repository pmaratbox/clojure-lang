# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. A `cond` over the three Ackermann cases captures the doubly-recursive definition cleanly.

## Run

    clojure -M main.clj
