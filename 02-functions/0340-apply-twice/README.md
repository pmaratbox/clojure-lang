# 0340 — Apply Twice

Write applyTwice(f,x)=f(f(x)) and apply it with inc to 3, printing `5`. Functions are first-class, so `inc` is passed straight to the higher-order `apply-twice`.

## Run

    clojure -M main.clj
