# 0139 — Recursive Power

Compute 2 raised to the 10th power recursively and print `1024`. A self-referential `defn` with a `zero?` base case expresses the recurrence idiomatically.

## Run

    clojure -M main.clj
