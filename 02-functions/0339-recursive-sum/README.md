# 0339 — Recursive List Sum

Recursively sum the list [1,2,3,4], printing `10`. Clojure uses `first`/`rest` and an `empty?` base case for the classic head-plus-tail recursion.

## Run

    clojure -M main.clj
