# 0390 — Big-Endian Bytes

Encode the integer 258 as two big-endian bytes (1, 2), decode them back to 258, printing `1 2 258`. Clojure uses `quot` and `mod` to split and recombine the bytes.

## Run

    clojure -M main.clj
