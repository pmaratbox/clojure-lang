# 0124 — Path Manipulation

Join "/tmp" and "file.txt", then take the basename and extension, printing `/tmp/file.txt file.txt .txt`. Use `java.nio.file.Paths` to join and inspect the path.

## Run

    clojure -M main.clj
