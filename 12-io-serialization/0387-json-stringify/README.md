# 0387 — JSON Stringify

Serialize the object {name:"Ada", age:36} to compact JSON `{"name":"Ada","age":36}`. Clojure builds the compact string with `str` to avoid stdlib spacing.

## Run

    clojure -M main.clj
