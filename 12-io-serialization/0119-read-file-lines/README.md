# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. Use `spit`/`slurp` for whole-file IO and `re-seq` to count non-empty lines.

## Run

    clojure -M main.clj
