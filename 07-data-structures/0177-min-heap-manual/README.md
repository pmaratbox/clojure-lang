# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. We back the heap with an immutable vector and write `sift-up`/`sift-down` as pure functions that return a new vector.

## Run

    clojure -M main.clj
