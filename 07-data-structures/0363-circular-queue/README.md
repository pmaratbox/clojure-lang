# 0363 — Circular Queue

In a capacity-3 circular queue enqueue 1,2,3, dequeue once, enqueue 4, then print the contents `2 3 4`. An `atom` over a fixed vector with head/count indices wraps positions with `mod`.

## Run

    clojure -M main.clj
