# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. Each `future` derefs the previous one, composing the asynchronous steps in sequence.

## Run

    clojure -M main.clj
