# 0234 — Parse Number Bases

Parse "ff" as hexadecimal (255) and "101" as binary (5), printing `255 5`. Clojure uses `Long/parseLong` with an explicit radix for each base.

## Run

    clojure -M main.clj
