# 0226 — Roman to Integer

Parse the Roman numeral "XIV" to its value `14`. Clojure pairs each value with its successor via `map` and negates when a smaller numeral precedes a larger.

## Run

    clojure -M main.clj
