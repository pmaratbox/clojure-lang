# 0221 — Tokenizer

Tokenize the expression "1 + 2" into token types, printing `NUM PLUS NUM`. Clojure threads the char sequence through `map`/`remove` and joins with `interpose`.

## Run

    clojure -M main.clj
