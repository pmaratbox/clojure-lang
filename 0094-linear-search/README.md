# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. `keep-indexed` yields the indices where the value matches; `first` takes the earliest — a linear search.

## Run

    clojure -M main.clj
