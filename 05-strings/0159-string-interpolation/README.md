# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. Clojure has no string interpolation, so `format` with `%s`/`%d` fills the template.

## Run

    clojure -M main.clj
