# 0355 — Expand Character Range

Expand the range spec "a-e" into the characters `abcde`. Mapping `char` over an inclusive `range` of code points builds the expanded string.

## Run

    clojure -M main.clj
