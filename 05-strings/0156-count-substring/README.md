# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. A loop/recur scan advances past each match by the needle length.

## Run

    clojure -M main.clj
