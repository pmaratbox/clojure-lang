# 0354 — Snake to Camel Case

Convert "hello_world" to camelCase `helloWorld`. Splitting on underscores then capitalizing the first char of each later word with `Character/toUpperCase` builds the result.

## Run

    clojure -M main.clj
