# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. `frequencies` builds a char->count map and `max-key val` picks the winner (l appears twice).

## Run

    clojure -M main.clj
