# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. `distinct` keeps a seen-set internally and preserves first-seen order.

## Run

    clojure -M main.clj
