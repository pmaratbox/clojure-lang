# 0353 — Character Frequency

Count character frequencies in "aab" and print them in first-seen order `a:2 b:1`. An `array-map` accumulated with `reduce` preserves first-appearance insertion order.

## Run

    clojure -M main.clj
