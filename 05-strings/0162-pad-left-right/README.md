# 0162 — Pad Left and Right

Left-pad and right-pad "5" with spaces to width 3 and print both joined by a pipe: `  5|5  `. `format` with `%3s` and `%-3s` pads to the right and left respectively.

## Run

    clojure -M main.clj
