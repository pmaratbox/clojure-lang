# 0157 — Longest Common Prefix

Find the longest common prefix of ["flower","flow","flight"], printing `fl`. A `for` with `:while` compares characters column-by-column until a mismatch.

## Run

    clojure -M main.clj
