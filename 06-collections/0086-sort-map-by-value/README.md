# 0086 — Sort a Map by Value

Sort the map `{a: 3, b: 1, c: 2}` by value in ascending order and print the entries: `b:1 c:2 a:3`. `sort-by val` orders the map's entries by their value; each `[k v]` pair is then formatted.

## Run

    clojure -M main.clj
