# 0358 — Adjacent Pairs

Produce the consecutive pairs of [1,2,3,4], printing `1,2 2,3 3,4`. `(partition 2 1 xs)` slides a size-2 window with step 1 over the sequence.

## Run

    clojure -M main.clj
