# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. Using a set as a predicate with `remove` keeps the original order while dropping members of the second list.

## Run

    clojure -M main.clj
