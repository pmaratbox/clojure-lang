# 0357 — Zip Longest

Zip [1,2,3] with ["a","b"], filling the missing slot with "-", printing `1a 2b 3-`. Clojure's `nth` takes a not-found default, so the shorter list yields "-" past its end.

## Run

    clojure -M main.clj
