# 0167 — Top-K Frequent

Find the 2 most frequent elements of [a,b,a,c,b,a], printing `a b`. `frequencies` builds the counts and `sort-by val >` orders them so we can take the top two.

## Run

    clojure -M main.clj
