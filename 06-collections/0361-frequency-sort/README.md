# 0361 — Frequency Sort

Sort [1,1,2,3,3,3] by descending frequency (ties keep first-seen order), printing `3 3 3 1 1 2`. `frequencies` counts occurrences and a stable `sort-by` over `distinct` keeps ties in first-seen order.

## Run

    clojure -M main.clj
