# 0166 — Cartesian Product

Print the cartesian product of [1,2] and ["a","b"] as `1a 1b 2a 2b`. A `for` comprehension with two bindings iterates the first list outer and the second inner.

## Run

    clojure -M main.clj
