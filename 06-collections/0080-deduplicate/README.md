# 0080 — Deduplicate

Remove duplicates from `1, 2, 2, 3, 1`, keeping the first occurrence of each in order, and print `1 2 3`. `distinct` returns a lazy sequence of the unique values in first-seen order.

## Run

    clojure -M main.clj
