# 0360 — Interleave Lists

Interleave [1,3,5] and [2,4,6] element by element, printing `1 2 3 4 5 6`. Core `interleave` alternates taking one element from each sequence.

## Run

    clojure -M main.clj
