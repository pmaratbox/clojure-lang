# 0084 — Partition

Partition `1, 2, 3, 4, 5, 6` into evens and odds, printing `evens: 2 4 6` and `odds: 1 3 5`. `group-by even?` produces a map keyed by `true`/`false`; the two groups are looked up by those keys.

## Run

    clojure -M main.clj
