# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. `group-by` keyed on each word's sorted letters partitions the words in one expression.

## Run

    clojure -M main.clj
