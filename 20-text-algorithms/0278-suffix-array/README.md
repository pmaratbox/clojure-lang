# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. `sort-by` keyed on each suffix's `subs` text yields the ordering declaratively.

## Run

    clojure -M main.clj
