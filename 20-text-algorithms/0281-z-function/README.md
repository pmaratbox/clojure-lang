# 0281 — Z-Function

Compute the Z-array of "aaaa" (excluding z[0]) and print `3 2 1`. A `loop`/`recur` scan carries the [l,r] Z-box while a primitive `int-array` holds the results.

## Run

    clojure -M main.clj
