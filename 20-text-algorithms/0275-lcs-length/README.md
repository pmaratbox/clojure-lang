# 0275 — Longest Common Subsequence

Compute the LCS length of "ABCBDAB" and "BDCAB", printing `4`. A rolling DP row carried through `loop`/`recur` keeps the table immutable.

## Run

    clojure -M main.clj
