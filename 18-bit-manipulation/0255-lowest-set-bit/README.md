# 0255 — Lowest Set Bit

Isolate the lowest set bit of 12 (1100), printing `4`. `(bit-and x (- x))` isolates the lowest set bit using Clojure's arbitrary-precision integers.

## Run

    clojure -M main.clj
