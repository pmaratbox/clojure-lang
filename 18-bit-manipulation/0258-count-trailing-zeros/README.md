# 0258 — Count Trailing Zeros

Count the trailing zero bits of 8 (1000), printing `3`. The JVM interop `Long/numberOfTrailingZeros` gives the answer with no hand-rolled loop.

## Run

    clojure -M main.clj
