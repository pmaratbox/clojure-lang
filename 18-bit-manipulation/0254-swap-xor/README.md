# 0254 — XOR Swap

Swap the values 3 and 5 using only XOR (no temporary), printing `5 3`. Successive `let` rebindings model the three-step XOR swap without any mutable variable.

## Run

    clojure -M main.clj
