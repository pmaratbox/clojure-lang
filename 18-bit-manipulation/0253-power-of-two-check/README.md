# 0253 — Power of Two Check

Test whether 16 (yes) and 18 (no) are powers of two, printing `yes no`. The predicate `(zero? (bit-and n (dec n)))` reads naturally with Clojure's `?`-suffixed boolean fns.

## Run

    clojure -M main.clj
