# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. Maps carry a `:tag` keyword and `case` dispatches the recursive eval idiomatically.

## Run

    clojure -M main.clj
