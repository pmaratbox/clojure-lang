# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. A vector of keywords gives declaration order, with `.indexOf` for the ordinal and `nth` for lookup by position.

## Run

    clojure -M main.clj
