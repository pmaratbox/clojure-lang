# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Records nest naturally, and keyword access composes to reach the inner field.

## Run

    clojure -M main.clj
