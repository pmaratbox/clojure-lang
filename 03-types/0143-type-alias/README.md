# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. Clojure is dynamically typed, so an alias is expressed by a named binding (with an optional `^long` hint) rather than a distinct type.

## Run

    clojure -M main.clj
