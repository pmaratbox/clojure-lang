# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. Clojure's `=` is value-based and recurses into nested persistent collections.

## Run

    clojure -M main.clj
