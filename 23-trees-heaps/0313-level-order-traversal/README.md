# 0313 — Level-Order Traversal

Print the breadth-first (level-order) traversal of the BST built from 5,3,8,1,4 `5 3 8 1 4`. Idiomatic Clojure runs a `loop`/`recur` BFS using a vector as the queue.

## Run

    clojure -M main.clj
