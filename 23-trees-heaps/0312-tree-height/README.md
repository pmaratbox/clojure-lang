# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. Idiomatic Clojure recurses with `1 + (max left right)` over nested maps.

## Run

    clojure -M main.clj
