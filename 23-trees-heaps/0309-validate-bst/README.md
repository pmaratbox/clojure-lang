# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. Idiomatic Clojure recurses over nested maps carrying the `(low high)` bounds.

## Run

    clojure -M main.clj
