# 0314 — Iterative Preorder

Print the iterative (stack-based) preorder traversal of the BST built from 5,3,8,1,4 `5 3 1 4 8`. Idiomatic Clojure uses a vector as the stack with `peek`/`pop`, pushing right before left.

## Run

    clojure -M main.clj
