# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. Idiomatic Clojure folds the data into a size-k min-heap (a kept-sorted vector) whose root is the answer.

## Run

    clojure -M main.clj
