# 0315 — Tree Diameter

Compute the diameter (longest path in edges) of a tree where root has children A and B, and A has children C and D, printing `3`. Idiomatic Clojure tracks the best span in an `atom` while computing subtree heights.

## Run

    clojure -M main.clj
