# 0311 — BST Delete

Insert 5,3,8,1,4 into a BST, delete 3, and print the in-order traversal `1 4 5 8`. Idiomatic Clojure deletes by recursively rebuilding nested maps using the in-order successor.

## Run

    clojure -M main.clj
