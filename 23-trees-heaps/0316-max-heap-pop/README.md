# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. Idiomatic Clojure stores the heap in a persistent vector and sifts down with `loop`/`recur`.

## Run

    clojure -M main.clj
