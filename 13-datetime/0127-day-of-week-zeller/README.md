# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. Idiomatic Clojure uses destructuring and `nth` into a vector of weekday names.

## Run

    clojure -M main.clj
