# 0394 — Add Minutes to Time

Add 90 minutes to 10:45 and print the resulting 24-hour time `12:15`. Clojure uses `quot`/`mod` on total minutes and zero-pads with `format`.

## Run

    clojure -M main.clj
