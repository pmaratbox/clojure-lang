# 0130 — Format ISO Date

Format the date y=2026, m=6, d=4 as a zero-padded ISO string `2026-06-04`. Idiomatic Clojure uses `format` with zero-padded width specifiers.

## Run

    clojure -M main.clj
