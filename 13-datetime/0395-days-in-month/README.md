# 0395 — Days in Month

Print the number of days in February 2000 (29) and February 2001 (28) as `29 28`. Clojure encodes the leap-year rule in a small `leap?` predicate.

## Run

    clojure -M main.clj
