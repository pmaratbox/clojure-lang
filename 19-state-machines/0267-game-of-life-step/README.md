# 0267 — Game of Life Step

Advance one step of a vertical blinker on a 3x3 grid (it becomes horizontal); print the grid using . and #. A `for` comprehension over neighbor offsets computes each cell's next state.

## Run

    clojure -M main.clj
