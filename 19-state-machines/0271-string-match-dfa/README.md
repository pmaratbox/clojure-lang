# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. A `loop`/`recur` walks the DFA states over the text and reports the start of the first accept.

## Run

    clojure -M main.clj
