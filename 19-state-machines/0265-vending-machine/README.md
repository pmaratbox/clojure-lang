# 0265 — Vending Machine

Insert coins 10, 10, 5 to reach the price 25 and dispense, printing `dispensed`. A `loop`/`recur` accumulator transitions to dispensing once the total meets the price.

## Run

    clojure -M main.clj
