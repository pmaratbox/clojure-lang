# 0409 — Distinct Until Changed

Implement distinctUntilChanged, dropping consecutive duplicate values from 1,1,2,2,2,3,1. An atom holds the last emitted value, forwarding only when the new value differs.

## Run

    clojure -M main.clj
