# 0413 — Zip Streams

Implement zip that pairs values by index and combines them; zip [1,2,3] with [10,20,30] using a+b. Per-source queues are atoms holding vectors, drained whenever both are non-empty.

## Run

    clojure -M main.clj
