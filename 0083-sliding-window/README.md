# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. `(partition 2 1 nums)` makes length-2 groups stepping by 1 — overlapping windows, dropping any short tail.

## Run

    clojure -M main.clj
