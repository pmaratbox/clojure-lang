# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `clojure.string/index-of` returns the index of the first match, or `nil` if the substring is absent.

## Run

    clojure -M main.clj
