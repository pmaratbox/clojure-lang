# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. The factory is a function returning a map of related products keyed by widget.

## Run

    clojure -M main.clj
