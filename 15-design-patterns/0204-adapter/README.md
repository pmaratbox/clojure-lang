# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. The adapter is a function wrapping the source map and exposing a `:fahrenheit` reading.

## Run

    clojure -M main.clj
