# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The shape holds the color implementor as a function, decoupling abstraction from implementation.

## Run

    clojure -M main.clj
