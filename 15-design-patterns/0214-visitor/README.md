# 0214 — Visitor

Use a visitor to sum the values of a small node tree with leaves 1, 2, 3, printing `6`. The visitor is a recursive function that accumulates each node's value as it walks the tree.

## Run

    clojure -M main.clj
