# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. A single recursive `size` function dispatches on the node's `:type` keyword.

## Run

    clojure -M main.clj
