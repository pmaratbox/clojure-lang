# 0205 — Decorator

Decorate a base coffee (cost 2) with milk (+1) and sugar (+1), printing the total cost `4`. Each decorator is a higher-order function wrapping the component's cost thunk.

## Run

    clojure -M main.clj
