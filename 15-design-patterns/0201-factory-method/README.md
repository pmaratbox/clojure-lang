# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. A `case` dispatch over the name returns plain maps acting as the products.

## Run

    clojure -M main.clj
