# 0218 — Flyweight

Share flyweight instances for repeated characters a, b, a and print the number of distinct objects created `2`. A cache atom memoizes one instance per key, so the distinct count is the cache size.

## Run

    clojure -M main.clj
