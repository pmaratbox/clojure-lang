# 0069 — Fibonacci (Iterative)

Print the first seven Fibonacci numbers, computed iteratively (each the sum of the previous two): `0 1 1 2 3 5 8`. `loop`/`recur` carries the pair `a`/`b`, a countdown `n`, and the accumulator, rolling forward without mutation.

## Run

    clojure -M main.clj
