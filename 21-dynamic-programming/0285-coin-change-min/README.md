# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. Idiomatic Clojure builds the DP table with `reduce` over a persistent map.

## Run

    clojure -M main.clj
