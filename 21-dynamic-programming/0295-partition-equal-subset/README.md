# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. The problem reduces to a subset-sum over a persistent set of reachable totals.

## Run

    clojure -M main.clj
