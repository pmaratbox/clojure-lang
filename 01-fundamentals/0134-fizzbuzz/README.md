# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. Uses `cond` over `(range 1 16)` with `mod` checks.

## Run

    clojure -M main.clj
