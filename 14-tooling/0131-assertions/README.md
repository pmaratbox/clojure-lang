# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A tiny `check` helper throws via `ex-info` when actual and expected differ.

## Run

    clojure -M main.clj
