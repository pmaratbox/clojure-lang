# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. An atom holds the count and `swap!` increments it on each invocation.

## Run

    clojure -M main.clj
