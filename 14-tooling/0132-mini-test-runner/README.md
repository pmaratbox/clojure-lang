# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. Each test is a closure in a vector; running them and tallying booleans gives the summary.

## Run

    clojure -M main.clj
