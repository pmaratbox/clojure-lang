# 0399 — Progress Bar

Render a width-10 progress bar at 40% completion, printing `[####------]`. `repeat` builds the filled and empty segments which `apply str` concatenates.

## Run

    clojure -M main.clj
