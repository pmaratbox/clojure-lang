# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A `defrecord` holds the fields and a small formatter renders the structured string.

## Run

    clojure -M main.clj
