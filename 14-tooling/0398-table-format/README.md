# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. The max first-column width drives a `format` left-justify, then cells join with " | ".

## Run

    clojure -M main.clj
