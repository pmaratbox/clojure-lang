# 0249 — Binomial Coefficient

Compute C(5,2) without overflow and print `10`. A `reduce` builds the product multiplicatively, dividing as it goes so intermediates stay small.

## Run

    clojure -M main.clj
