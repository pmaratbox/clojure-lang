# 0238 — Fraction Addition

Add the fractions 1/2 and 1/3 and print the reduced result `5/6`. A small `gcd` via `recur` reduces the cross-multiplied numerator and denominator.

## Run

    clojure -M main.clj
