# 0242 — Matrix-Vector Product

Multiply [[1,2],[3,4]] by the vector [5,6], printing `17 39`. Each output entry is the dot product of a row with the vector via `map` and `apply +`.

## Run

    clojure -M main.clj
