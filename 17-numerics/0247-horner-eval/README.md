# 0247 — Horner Polynomial Eval

Evaluate 2x^2+3x+1 at x=2 using the Horner method, printing `15`. A single `reduce` over the coefficients folds the nested multiply-add.

## Run

    clojure -M main.clj
