# 0325 — Point-Free Style

Express "sum of squares" point-free (compose map-square with sum) and apply it to [1,2,3], printing `14`. `comp` builds the function from `map` and `reduce` with no explicit argument names.

## Run

    clojure -M main.clj
