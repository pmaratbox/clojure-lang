# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. `(int \A)` returns a character's code point and `(char 66)` builds a character from a code.

## Run

    clojure -M main.clj
